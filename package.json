{
  "name": "ai-resume-analysis",
  "version": "1.0.0",
  "description": "A full-stack application that analyzes resumes against job descriptions using AI (Groq LLaMA), providing match scores, skill gap analysis, and career roadmaps.",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run db:generate --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
